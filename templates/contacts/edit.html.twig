{% extends 'base.html.twig' %}

{% block title %}Edit Contacts{% endblock %}

{% block content %}
    <h1>Edit Contacts</h1>

    {{ include('contacts/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('app_contacts_index') }}">back to list</a>

    {{ include('contacts/_delete_form.html.twig') }}
{% endblock %}
