{% extends 'base.html.twig' %}

{% block title %}HideOut{% endblock %}

{% block content %}
    <h1>Planque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hide_out.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ hide_out.code }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ hide_out.country }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ hide_out.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hide_out_index') }}">back to list</a>

    <a href="{{ path('app_hide_out_edit', {'id': hide_out.id}) }}">edit</a>

    {{ include('hide_out/_delete_form.html.twig') }}
{% endblock %}
